'use strict';

// 의존 모듈 로드
var gulp       = require('gulp');
var yargs      = require('yargs').argv;
var log        = require('./gulp_task/utils/log');
var $          = require('gulp-load-plugins')({'lazy': true});

// --------------------------------------------------
// 기본 업무 등록
// gulp.task('default', []);
